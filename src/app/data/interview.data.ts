import { InterviewQuestion } from '../models/types';

export const INTERVIEW_QUESTIONS: InterviewQuestion[] = [
  // JavaScript
  { id: 'js-1', skillId: 'javascript', question: 'Explain the difference between var, let, and const in JavaScript. When would you use each?', difficulty: 'easy', modelAnswer: 'var is function-scoped and hoisted, which can cause unexpected behavior. let is block-scoped and can be reassigned. const is block-scoped and cannot be reassigned (though objects/arrays it references can be mutated). Use const by default, let when you need to reassign, and avoid var in modern code.' },
  { id: 'js-2', skillId: 'javascript', question: 'What is the event loop in JavaScript? How does it handle asynchronous operations?', difficulty: 'medium', modelAnswer: 'The event loop is a mechanism that allows JavaScript (single-threaded) to perform non-blocking operations. It continuously checks the call stack and task queue. When the call stack is empty, it takes the first task from the queue and pushes it onto the stack. Microtasks (Promises) have priority over macrotasks (setTimeout). This enables async I/O without threads.' },
  { id: 'js-3', skillId: 'javascript', question: 'What are closures and how are they used in practice?', difficulty: 'medium', modelAnswer: 'A closure is a function that retains access to its outer scope variables even after the outer function has returned. They\'re used for data privacy (module pattern), creating factory functions, and maintaining state in callbacks. Example: a counter function that remembers its count between calls.' },

  // TypeScript
  { id: 'ts-1', skillId: 'typescript', question: 'What is the difference between an interface and a type alias in TypeScript? When would you prefer one over the other?', difficulty: 'easy', modelAnswer: 'Interfaces are extensible (can be merged/extended with extends), great for object shapes and class contracts. Type aliases are more flexible — they support unions, intersections, mapped types, and primitives. Use interfaces for object shapes and public APIs (they\'re more extendable), and type aliases for unions, complex types, or when you need computed properties.' },
  { id: 'ts-2', skillId: 'typescript', question: 'Explain generics in TypeScript with a practical example.', difficulty: 'medium', modelAnswer: 'Generics allow you to create reusable components that work with multiple types while maintaining type safety. Example: function identity<T>(arg: T): T { return arg; } — this works for any type without losing type information. Practical use: a generic API response wrapper like ApiResponse<T> { data: T; error: string | null; }.' },

  // React
  { id: 'react-1', skillId: 'react', question: 'Explain the difference between useState and useRef. When would you use each?', difficulty: 'easy', modelAnswer: 'useState triggers a re-render when updated and is for values that affect the UI. useRef persists values between renders WITHOUT causing re-renders — used for DOM references, storing previous values, or mutable values that don\'t need to trigger UI updates. Rule: if changing the value should update what the user sees, use useState.' },
  { id: 'react-2', skillId: 'react', question: 'What are React Server Components and how do they differ from Client Components?', difficulty: 'hard', modelAnswer: 'Server Components render on the server and send HTML to the client — they can directly access databases, have zero client-side JS bundle cost, and can\'t use hooks or browser APIs. Client Components render on the client, support interactivity (onClick, useState). Use Server Components by default for data fetching and static content; Client Components for interactivity.' },

  // Python
  { id: 'py-1', skillId: 'python', question: 'What are Python decorators and how do they work? Give a practical example.', difficulty: 'medium', modelAnswer: 'Decorators are functions that modify the behavior of other functions. They take a function as input and return a modified function. Syntax: @decorator above a function definition. Practical example: @login_required to check authentication before a view function runs, or @timer to measure execution time. Under the hood, @decorator def func() is equivalent to func = decorator(func).' },
  { id: 'py-2', skillId: 'python', question: 'Explain the difference between a list and a tuple in Python. When would you use each?', difficulty: 'easy', modelAnswer: 'Lists are mutable (can be changed after creation), tuples are immutable. Tuples are slightly faster and can be used as dictionary keys. Use lists when you need to modify the collection, tuples for fixed collections like coordinates (x, y), database rows, or function return values. Tuples also signal intent: "this data shouldn\'t change."' },

  // SQL
  { id: 'sql-1', skillId: 'sql', question: 'What is the difference between INNER JOIN, LEFT JOIN, and FULL OUTER JOIN?', difficulty: 'easy', modelAnswer: 'INNER JOIN returns only rows that match in both tables. LEFT JOIN returns all rows from the left table and matched rows from the right (NULLs for non-matches). FULL OUTER JOIN returns all rows from both tables, with NULLs where there\'s no match. Use INNER when you need only matching data, LEFT when you want all records from one side regardless of matches.' },
  { id: 'sql-2', skillId: 'sql', question: 'How would you optimize a slow SQL query? Walk through your debugging process.', difficulty: 'hard', modelAnswer: '1) Run EXPLAIN/EXPLAIN ANALYZE to see the query plan. 2) Look for full table scans — add indexes on columns used in WHERE, JOIN, ORDER BY. 3) Check for N+1 queries and replace with JOINs. 4) Avoid SELECT * — only fetch needed columns. 5) Check if statistics are up to date. 6) Consider partitioning for very large tables. 7) Use query caching where appropriate.' },

  // Docker
  { id: 'docker-1', skillId: 'docker', question: 'What is the difference between a Docker image and a container? How do they relate?', difficulty: 'easy', modelAnswer: 'An image is a read-only template containing the application code, runtime, libraries, and dependencies. A container is a running instance of an image — it adds a writable layer on top. Think of an image as a class and a container as an object. You can run multiple containers from the same image, each isolated from the others.' },
  { id: 'docker-2', skillId: 'docker', question: 'Explain multi-stage builds in Docker and why they\'re useful.', difficulty: 'medium', modelAnswer: 'Multi-stage builds use multiple FROM statements in a Dockerfile. Each stage can use a different base image. You compile/build in one stage (with build tools) and copy only the output to a final minimal stage (e.g., alpine). This dramatically reduces image size — a Node.js app might go from 1GB (with node_modules + dev tools) to 100MB (just the built app + runtime).' },

  // Kubernetes
  { id: 'k8s-1', skillId: 'kubernetes', question: 'What is a Pod in Kubernetes and why doesn\'t Kubernetes just run containers directly?', difficulty: 'easy', modelAnswer: 'A Pod is the smallest deployable unit in Kubernetes — it wraps one or more containers that share the same network namespace and storage. Kubernetes uses Pods (not raw containers) because some containers need to run together (sidecar pattern), share localhost networking, and share volumes. Example: an app container + a log shipping sidecar in the same Pod.' },
  { id: 'k8s-2', skillId: 'kubernetes', question: 'Explain the difference between a Deployment, StatefulSet, and DaemonSet.', difficulty: 'medium', modelAnswer: 'Deployment: manages stateless replicas with rolling updates (most common). StatefulSet: for stateful apps needing stable network identity and persistent storage (databases). DaemonSet: ensures one Pod runs on every node (monitoring agents, log collectors). Use Deployments for web apps, StatefulSets for databases, DaemonSets for cluster-wide agents.' },

  // AWS
  { id: 'aws-1', skillId: 'aws', question: 'Explain the difference between EC2, Lambda, and ECS. When would you choose each?', difficulty: 'medium', modelAnswer: 'EC2: virtual machines you manage — full control, any workload, you handle scaling/patching. Lambda: serverless functions — no servers to manage, pay-per-invocation, great for event-driven tasks under 15 min. ECS: managed container orchestration — run Docker containers without managing Kubernetes. Choose EC2 for legacy apps, Lambda for event-driven microservices, ECS for containerized applications.' },
  { id: 'aws-2', skillId: 'aws', question: 'What is an IAM role and how does it differ from an IAM user?', difficulty: 'easy', modelAnswer: 'IAM users have permanent credentials (username/password or access keys) and represent people. IAM roles have temporary credentials and can be assumed by users, services, or applications. Roles are more secure (no long-lived keys) and are used for cross-account access, EC2 instances accessing S3, Lambda functions, etc. Best practice: use roles over access keys whenever possible.' },

  // Terraform
  { id: 'tf-1', skillId: 'terraform', question: 'What is Terraform state and why is it important? What happens if you lose it?', difficulty: 'medium', modelAnswer: 'Terraform state is a JSON file that maps your .tf configuration to real infrastructure resources. It tracks what Terraform manages, enabling it to know what to create, update, or destroy. If you lose state, Terraform doesn\'t know about existing resources — it would try to recreate everything, causing conflicts. Best practice: store state remotely (S3 + DynamoDB for locking) and never edit it manually.' },

  // CI/CD
  { id: 'cicd-1', skillId: 'ci-cd', question: 'What is the difference between Continuous Integration, Continuous Delivery, and Continuous Deployment?', difficulty: 'easy', modelAnswer: 'CI (Continuous Integration): automatically build and test code on every commit. CD (Continuous Delivery): CI + automatically prepare releases that CAN be deployed (manual approval). CD (Continuous Deployment): CI + automatically deploy every passing commit to production (no manual step). Most teams do CI + Continuous Delivery, with manual approval for production deployments.' },

  // Data Science / ML
  { id: 'pandas-1', skillId: 'pandas', question: 'What is the difference between loc and iloc in Pandas?', difficulty: 'easy', modelAnswer: 'loc uses label-based indexing (column names, index labels): df.loc[0, "name"]. iloc uses integer position-based indexing: df.iloc[0, 1]. loc is inclusive on both ends of a slice, iloc is exclusive on the end (like Python lists). Use loc when you know column names, iloc when you need positional access.' },
  { id: 'sklearn-1', skillId: 'scikit-learn', question: 'Explain the bias-variance tradeoff. How does it affect model selection?', difficulty: 'medium', modelAnswer: 'Bias = error from simplifying assumptions (underfitting). Variance = error from sensitivity to training data (overfitting). High bias models (linear regression on non-linear data) miss patterns. High variance models (deep decision trees) memorize noise. The tradeoff: reducing one often increases the other. Solutions: cross-validation, regularization, ensemble methods, and choosing appropriate model complexity.' },
  { id: 'stats-1', skillId: 'statistics', question: 'Explain the difference between Type I and Type II errors. Which is worse?', difficulty: 'medium', modelAnswer: 'Type I (false positive): rejecting a true null hypothesis — "finding" an effect that doesn\'t exist. Type II (false negative): failing to reject a false null hypothesis — missing a real effect. Which is worse depends on context: in medical testing, Type II (missing cancer) is worse. In spam filtering, Type I (blocking good email) may be worse. You control Type I with significance level (α), Type II with statistical power.' },
  { id: 'dl-1', skillId: 'deep-learning', question: 'What is the vanishing gradient problem and how do modern architectures address it?', difficulty: 'hard', modelAnswer: 'In deep networks, gradients can become exponentially small as they backpropagate through layers, making early layers learn very slowly. Solutions: ReLU activation (doesn\'t saturate like sigmoid), batch normalization (normalizes layer inputs), residual connections/skip connections (ResNets — gradients flow directly through shortcuts), LSTM/GRU gates for RNNs, and proper weight initialization (He/Xavier).' },
  { id: 'tf-dl-1', skillId: 'tensorflow', question: 'What is transfer learning and when would you use it?', difficulty: 'medium', modelAnswer: 'Transfer learning uses a pre-trained model (trained on a large dataset like ImageNet) as a starting point for a new task. You freeze early layers (which learn general features like edges/textures) and fine-tune later layers on your specific data. Use it when: you have limited training data, your task is similar to what the model was pre-trained on, or you need faster training. Example: using ResNet50 pre-trained on ImageNet for medical image classification.' },

  // Git
  { id: 'git-1', skillId: 'git', question: 'What is the difference between git merge and git rebase? When would you use each?', difficulty: 'medium', modelAnswer: 'Merge creates a merge commit preserving the full branch history (non-destructive). Rebase replays your commits on top of the target branch, creating a linear history (rewrites history). Use merge for shared/public branches (main, develop) to preserve history. Use rebase for local feature branches before merging to keep history clean. Never rebase commits that have been pushed and shared with others.' },

  // REST APIs
  { id: 'rest-1', skillId: 'rest-api', question: 'What makes an API RESTful? Name the key constraints.', difficulty: 'easy', modelAnswer: 'REST constraints: 1) Client-server separation. 2) Stateless — each request contains all needed info. 3) Cacheable — responses can be cached. 4) Uniform interface — consistent URLs, HTTP methods (GET/POST/PUT/DELETE), and resource representations. 5) Layered system — client doesn\'t know if it\'s talking to the end server or middleware. 6) (Optional) Code on demand. Key: use nouns for URLs (/users, not /getUsers), proper HTTP status codes, and HATEOAS for discoverability.' },

  // Monitoring
  { id: 'mon-1', skillId: 'monitoring', question: 'What are the "Four Golden Signals" of monitoring and why are they important?', difficulty: 'medium', modelAnswer: 'From Google\'s SRE book: 1) Latency — time to serve a request (distinguish success vs. error latency). 2) Traffic — demand on the system (requests/sec). 3) Errors — rate of failed requests. 4) Saturation — how "full" the system is (CPU, memory, disk). These four signals cover the most critical aspects of service health. If you can only monitor four things, monitor these. Alert on these to catch issues before users notice.' },

  // Linux
  { id: 'linux-1', skillId: 'linux', question: 'How would you troubleshoot a Linux server that is running slowly?', difficulty: 'medium', modelAnswer: 'Systematic approach: 1) top/htop — check CPU and memory usage, find resource-hungry processes. 2) df -h — check disk space. 3) iostat — check disk I/O. 4) free -m — check memory/swap usage. 5) netstat/ss — check network connections. 6) dmesg — check kernel messages for errors. 7) journalctl — check system logs. 8) Check if it\'s a single process (kill/restart it) or system-wide (may need to add resources or investigate root cause).' },

  // Security
  { id: 'sec-1', skillId: 'security', question: 'What is SQL injection and how do you prevent it?', difficulty: 'easy', modelAnswer: 'SQL injection occurs when user input is concatenated directly into SQL queries, allowing attackers to execute arbitrary SQL. Example: " OR 1=1 -- " in a login form. Prevention: 1) Use parameterized queries/prepared statements (most important). 2) Use an ORM. 3) Input validation and sanitization. 4) Principle of least privilege for database accounts. 5) Web Application Firewall as defense in depth. Never concatenate user input into SQL strings.' },

  // Unit Testing
  { id: 'test-1', skillId: 'unit-testing', question: 'What is the difference between unit tests, integration tests, and end-to-end tests? How should they be balanced?', difficulty: 'easy', modelAnswer: 'Unit tests: test individual functions/classes in isolation (fast, many). Integration tests: test how components work together (APIs, DB queries — medium speed, fewer). E2E tests: test the full user flow through the real app (slow, fewest). The testing pyramid: many unit tests at the base, fewer integration in the middle, fewest E2E at the top. Aim for ~70% unit, ~20% integration, ~10% E2E.' },
];

